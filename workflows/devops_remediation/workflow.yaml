workflow_id: devops_remediation
name: DevOps File Analysis
description: >
  Reads a file via MCP filesystem server and produces a structured analysis.
  Demonstrates MCP stdio tool integration.
pattern: parallel_specialist
agent_ids:
  - file_analyst_agent
pattern_config:
  strategy: concatenate
